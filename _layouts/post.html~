---
layout: default
---
<article>

  <header>
    <h1>{{ page.title | escape }}</h1>
    <p class="text-muted">
      <time datetime="{{ page.date | date_to_xmlschema }}">
        {%- assign date_format = "%b %-d, %Y" -%}
        {{ page.date | date: date_format }}
      </time>
      {%- if page.author -%}
        • {{ page.author }}
      {%- endif -%}
    </p>
      <div><p>Categories:<span>[{%- for category in page.categories -%}
    			{%- capture category_name -%} {{category|url_encode}} {%- endcapture -%}
    			<a href="/category/{{category_name}}"><code class="highlighter-rouge">
    			<nobr>{{category_name}}</nobr></code></a>,
				{%-endfor-%}
    			]</span><p></div>
    <div><p>Tags:
    <span>[{%- for tag in page.tags -%}
    			{%- capture tag_name -%} {{tag|url_encode}} {%- endcapture -%}
    			<a href="/tag/{{tag_name}}"><code class="highlighter-rouge">
    			<nobr>{{tag_name}}</nobr></code></a>,
				{%-endfor-%}
    			]</span><p></div>
  </header>

  <section>
    {{ content }}
  </section>

</article>
